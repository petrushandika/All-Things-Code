package main

import "fmt"

func main() {
	// notes: constant cant change value of variable

	// using varible with const (constant) keyword
	const username string = "Petrus Handika"
	const address = "Jakarta"

	fmt.Println(username)
	fmt.Println(address)
}